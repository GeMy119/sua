import { MomentHijriPipe } from './moment-hijri.pipe';

describe('MomentHijriPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentHijriPipe();
    expect(pipe).toBeTruthy();
  });
});
